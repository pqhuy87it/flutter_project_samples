import 'package:flutter/material.dart';
import 'base_screen.dart';

class ScreenE extends StatelessWidget {
  const ScreenE({super.key});
  @override
  Widget build(BuildContext context) {
    return const BaseScreen(title: "E"); // Hết đường
  }
}